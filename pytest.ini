[pytest]
testpaths = tests
filterwarnings =
    ; starlette 1.0's test client still uses an anyio alias that anyio has
    ; deprecated - third-party noise between pinned versions, silenced by
    ; exact message so warnings from our own code still show up
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning
